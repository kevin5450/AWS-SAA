# SAA Study Roadmap

## 1. Networking
VPC → Subnet → Route Table → IGW/NAT → SG/NACL → Endpoint → Peering/TGW → VPN/DX

## 2. Compute
EC2 → AMI → EBS/Instance Store → ELB → ASG → Placement / Purchase Options

## 3. Storage
S3 → Storage Classes → Lifecycle → Replication → Transfer → EFS/FSx

## 4. Database
RDS/Aurora → DynamoDB → ElastiCache → Redshift

## 5. Security
IAM → KMS → Secrets Manager → WAF/Shield → Organizations

## 6. Integration / Serverless
Lambda → SQS → SNS → EventBridge → Step Functions → API Gateway

## 7. Containers
ECR → ECS → Fargate → EKS

## 8. Monitoring / Governance
CloudWatch → CloudTrail → Config → Systems Manager

## 9. Hybrid
Site-to-Site VPN → Client VPN → Direct Connect → DXGW → TGW
